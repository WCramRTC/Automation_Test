name: Will Test

run-name: ${{github.actor}} is learning GitHub Actions

on:
  push: 
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Main
      run: actions/checkout@v4

    - name: Installing Python
      run: actions/setup-python@5